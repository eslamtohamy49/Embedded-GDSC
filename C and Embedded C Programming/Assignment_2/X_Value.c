#include <stdio.h>

int main()
{

    signed char x = -6;
    signed char y;

    // Right shift by 2 bits
    y = x >> 2;

    // Print the result
    printf("The value of y is: %d\n", y);


    return 0;
}
